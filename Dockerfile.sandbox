FROM python:3.11-slim
RUN apt-get update && apt-get install -y --no-install-recommends git grep nodejs npm golang-go && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir pytest ruff flake8
WORKDIR /work
CMD ["sleep", "infinity"]
